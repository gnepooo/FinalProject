@model List<FinalProject.Models.Quiz>

<h2>Available Quizzes</h2>

<ul>
    @foreach (var quiz in Model)
    {
        <li>
            <a asp-action="Take" asp-route-id="@quiz.Id">@quiz.Title</a>
        </li>
    }
</ul>
